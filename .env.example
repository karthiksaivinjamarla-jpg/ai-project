# Optional server-side AI gateway. Keep provider API keys on the server, never in VITE_* variables.
# The gateway receives { operation, ...payload } and returns the matching AIService result shape.
VITE_AI_ENDPOINT=
